class Solution {
public:
    int minimumSum(int num) {
        vector<int> v;
        int n = 4;
        while(n)
        {
            v.push_back(num%10);
            num /= 10;
            --n;
        }
        sort(v.begin(), v.end());
        
        int res = (v[0]*10 + v[2]) + (v[1]*10 + v[3]);
        return res;
    }
};
